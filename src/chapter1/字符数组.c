#include<stdio.h>
#define MAXLINE 1000

int getLine(char line[], int maxline);
void copy(char to[], char from[]);

int main(void)
{
	int max = 0;
	char line[MAXLINE];
	char longest[MAXLINE];


	return 0;
}

int getLine(char line[], int maxline)
{
	char c;
	int i, c;
	for ()
	return 0;
}
